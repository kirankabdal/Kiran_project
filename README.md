# Kiran_project
Author - Kiran Kabdal (ThousandEyes)

If you're using the VSCode then to import all the libraries please use the following:

/usr/local/bin/python3 -m venv venv
source venv/bin/activate
pip install requests pandas openpyxl (depends on which version you're using) 
